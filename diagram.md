# Architecture diagram moved

The active architecture description and Mermaid diagram now live in [`docs/architecture.md`](docs/architecture.md).

This file is kept as a compatibility pointer because older notes referenced `diagram.md`.
